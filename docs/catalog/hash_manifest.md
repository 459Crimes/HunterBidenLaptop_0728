# Catalog: hash manifest

> Parent: [Evidence catalog](README.md). Articles: [Integrity](../INTEGRITY.md) · [Copy lineages](../COPY_LINEAGES.md).

| File | Role |
|---|---|
| [`04_coverage.tsv`](../../build/hash_manifest/04_coverage.tsv) | Unique-hash overlap vs APFS (1), GAI (116), JPMI (122) |
| [`05_overlap_by_primary.tsv`](../../build/hash_manifest/05_overlap_by_primary.tsv) | Same numbers, one row per join |

Per-hash flag dumps belong in gitignored `build/deep/`. Hashes are **received inventory evidence** unless recomputed from restricted source bytes.
